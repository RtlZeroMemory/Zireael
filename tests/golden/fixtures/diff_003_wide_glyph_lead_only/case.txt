# fixture_id: diff_003_wide_glyph_lead_only
# purpose: wide glyph emission prints only lead cell bytes; dirty continuation includes lead.
#
# pinned:
#   - caps.color_mode = RGB (truecolor)
#   - caps.sgr_attrs_supported = all
#   - initial_term_state.cursor = (0,0)
#   - initial_term_state.style = default (fg=0 bg=0 attrs=0)
#
# prev: "    "
# next: " 🙂~ "  (lead at x=1, continuation at x=2)

